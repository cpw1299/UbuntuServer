## 🔊 free-claude-code 完整 .env 配置项中文语音描述

### 🔧 基础运行设置
| 配置项 | 通俗解释 | 是否必填 |
|--------|----------|----------|
| `PORT` | 本地代理监听的端口号，默认 `8082`。一般不用改，除非端口冲突了。 | 可选 |
| `HOST` | 监听地址，默认 `0.0.0.0` 表示本机所有网卡都能访问。如果只想让本机自己用就改成 `127.0.0.1`。 | 可选 |
| `HTTP_TIMEOUT` | HTTP 请求超时时间，单位是秒。比如填 `120` 表示等 2 分钟没结果就放弃。 | 可选 |

### 🧠 模型映射核心（Claude 模型 → 实际后端模型）
> 注释原话：All Claude model requests are mapped to these models, plain model is fallback

| 配置项 | 通俗解释 | 是否必填 |
|--------|----------|----------|
| `MODEL_OPUS` | 当 Claude Code 调用 **Opus** 等级模型时，实际转发到哪个模型。例如填 `aliyun/qwen-max-latest`。 | 必填（至少需要一组映射） |
| `MODEL_SONNET` | 转发 **Sonnet** 等级模型的请求。日常绝大多数对话走这个。 | 必填 |
| `MODEL_HAIKU` | 转发 **Haiku** 等级轻量模型的请求，一般用于摘要、快速检索等。 | 必填 |
| `MODEL` | 兜底映射。如果上面的细分模型没配，就用这个。可以只配这一个，不用单独配上面三个。 | 建议填写 |

### 🧪 冒烟测试模型覆盖（启动时健康检查用）
> 注释原话：Optional live smoke model overrides. Provider smoke runs once per configured

| 配置项 | 通俗解释 | 是否必填 |
|--------|----------|----------|
| `SMOKE_MODEL_OPUS` | 启动时单独用来测试 Opus 通道是否可用的模型，覆盖主配置。如果空就用上面的 `MODEL_OPUS` 来测。 | 可选 |
| `SMOKE_MODEL_SONNET` | 测试 Sonnet 通道的模型覆盖。 | 可选 |
| `SMOKE_MODEL_HAIKU` | 测试 Haiku 通道的模型覆盖。 | 可选 |

### 💭 思维链输出控制
> 注释原话：Thinking output

| 配置项 | 通俗解释 | 是否必填 |
|--------|----------|----------|
| `THINKING_OUTPUT` | 是否让 Claude Code 显示模型的“内心思考过程”（如果后端模型支持）。填 `true` 或 `1` 开启，填 `false` 或留空关闭。 | 可选 |

### 🔐 各类平台 API Key
| 配置项 | 通俗解释 | 是否必填 |
|--------|----------|----------|
| `NVIDIA_NIM_API_KEY` | NVIDIA NIM 免费额度的钥匙，`nvapi-` 开头。不用就留空。 | 按需 |
| `OPENROUTER_API_KEY` | OpenRouter 的 API Key，可调用很多商业/开源模型。 | 按需 |
| `DEEPSEEK_API_KEY` | DeepSeek 官方 API Key。 | 按需 |
| `GROQ_API_KEY` | Groq 云平台 API Key，速度快，有免费额度。 | 按需 |
| `GITHUB_TOKEN` | 用 GitHub Models 时需要，格式 `ghp_xxx`。 | 按需 |
| `OLLAMA_HOST` | 如果你本地跑了 Ollama，这里填地址，比如 `http://localhost:11434`。 | 按需 |
| `ALIYUN_API_KEY` | 阿里云百炼的 API Key，`sk-` 开头。 | 按需 |
| `CUSTOM_API_KEY` | 自建 OpenAI 兼容接口的密钥。 | 按需 |

### 🧩 供应商专属模型映射（可覆盖通用映射）
| 配置项 | 通俗解释 | 是否必填 |
|--------|----------|----------|
| `ALIYUN_MODEL_OPUS` | 百炼体系下，Opus 等级具体映射哪个模型（优先级高于 `MODEL_OPUS`）。 | 可选 |
| `ALIYUN_MODEL_SONNET` | 百炼 Sonnet 等级映射。 | 可选 |
| `ALIYUN_MODEL_HAIKU` | 百炼 Haiku 等级映射。 | 可选 |
| `CUSTOM_PROVIDER_URL` | 自建 OpenAI 兼容接口的完整地址，比如 `http://your-proxy/v1`。 | 按需 |

### 🔑 代理服务器自身安全
> 注释原话：Optional server API key (Anthropic-style)

| 配置项 | 通俗解释 | 是否必填 |
|--------|----------|----------|
| `SERVER_API_KEY` | 给代理加把锁，客户端请求时必须带上这个 Key（格式类似 Anthropic 的认证头）。如果不填，代理不需要认证，谁都能连。 | 可选（建议生产环境配） |

### 💬 即时通讯平台集成
> 注释原话：Messaging Platform: "telegram" | "discord" | "none"

| 配置项 | 通俗解释 | 是否必填 |
|--------|----------|----------|
| `MESSAGING_PLATFORM` | 选择你想用的聊天工具，填 `telegram`、`discord` 或 `none`。填了之后代理可以接收 Telegram/Discord 的消息并调用 Claude Code。 | 可选 |

#### 📨 Telegram 专属配置
| 配置项 | 通俗解释 | 是否必填 |
|--------|----------|----------|
| `TELEGRAM_BOT_TOKEN` | Telegram 机器人的 Token，找 BotFather 申请。 | 用 Telegram 时必填 |
| `TELEGRAM_ALLOWED_USERS` | 允许使用机器人的 Telegram 用户 ID，逗号分隔。不填则所有用户都能用（尽量限制）。 | 可选 |

#### 🎮 Discord 专属配置
| 配置项 | 通俗解释 | 是否必填 |
|--------|----------|----------|
| `DISCORD_BOT_TOKEN` | Discord 机器人的 Token。 | 用 Discord 时必填 |
| `DISCORD_ALLOWED_CHANNELS` | 允许机器人响应的频道 ID 列表，逗号分隔。 | 可选 |
| `DISCORD_ALLOWED_USERS` | 允许使用机器人的 Discord 用户 ID。 | 可选 |

### 🎤 语音笔记转录
| 配置项 | 通俗解释 | 是否必填 |
|--------|----------|----------|
| `TRANSCRIPTION_PROVIDER` | 语音转文字用哪个服务，例如 `whisper` 或 `openai`。 | 需要语音功能时填 |
| `TRANSCRIPTION_API_KEY` | 对应转录服务的 API Key。 | 按需 |
| `TRANSCRIPTION_MODEL` | 使用的转录模型名称，如 `whisper-1`。 | 可选 |

### 🤖 Agent 配置
| 配置项 | 通俗解释 | 是否必填 |
|--------|----------|----------|
| `AGENT_ENABLED` | 是否开启 Agent 模式，让模型能自主调用工具、执行多步任务。填 `true` 或 `1` 开启。 | 可选 |
| `AGENT_MAX_STEPS` | Agent 最多连续执行多少步操作，防止无限循环。 | 可选 |
| `AGENT_TOOLS` | 允许 Agent 使用哪些工具，比如 `read_file,write_file,exec_command`，通常用默认就行。 | 可选 |

### 🌐 本地网络搜索/抓取
> 注释原话：Local Anthropic web_search / web_fetch handling (performs outbound HTTP; on by default)

| 配置项 | 通俗解释 | 是否必填 |
|--------|----------|----------|
| `LOCAL_WEB_SEARCH` | 是否允许 Claude Code 直接从你的电脑发起网络搜索和网页抓取。默认开启（`true`），如果你担心隐私或网络环境，可以设成 `false` 关闭。 | 可选 |

> 💡 所有空白项都表示“不启用对应功能”，放心留空，不影响核心代理使用。